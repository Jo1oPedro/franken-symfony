security:
    password_hashers:
        App\Identity\Entity\User: 'auto'

    providers:
        users:
            entity:
                class: App\Identity\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false

        api:
            pattern: ^/api
            stateless: true
            provider: users
            jwt: ~

    access_control:
        - { path: ^/api/register, roles: PUBLIC_ACCESS }
        - { path: ^/api/login,    roles: PUBLIC_ACCESS }
        - { path: ^/api,          roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            App\Identity\Entity\User:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10
